import React from 'react'

export const capitalize = someText => someText.toUpperCase().split(' ')

const Example = () => (
  <div>
    no component for this example :(
  </div>
)

export default Example
